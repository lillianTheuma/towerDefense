export default class towerTypes {
  constructor() {
    this.types = [];
  }
  findTowerByID(id) {
    this.towers.forEach(function(tower) {
      if tower.id = id {
        return tower;
      }
    });
  retu
}
